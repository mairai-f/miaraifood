import { useEffect, useState } from 'react';
type InstallEvent = Event & { prompt: () => Promise<void>; userChoice: Promise<{ outcome: 'accepted' | 'dismissed' }> };
export function InstallPrompt() { const [event, setEvent] = useState<InstallEvent | null>(null); const [visible, setVisible] = useState(false); useEffect(() => { if (window.matchMedia('(display-mode: standalone)').matches || sessionStorage.getItem('miar-install-dismissed')) return; setVisible(true); const handler = (value: Event) => { value.preventDefault(); setEvent(value as InstallEvent); }; window.addEventListener('beforeinstallprompt', handler); return () => window.removeEventListener('beforeinstallprompt', handler); }, []); if (!visible) return null; const install = async () => { if (!event) return; await event.prompt(); if ((await event.userChoice).outcome === 'accepted') setVisible(false); setEvent(null); }; return <aside role="status" className="fixed bottom-4 left-4 right-4 z-50 mx-auto flex max-w-md items-center justify-between gap-3 rounded-xl border border-emerald-500/30 bg-slate-900 px-4 py-3 text-sm text-slate-100 shadow-xl"><span>Instale a MIAR neste aparelho.</span><div className="flex gap-2"><button type="button" onClick={() => void install()} disabled={!event} className="rounded-lg bg-emerald-500 px-3 py-1.5 font-semibold text-slate-950 disabled:opacity-50">Instalar</button><button type="button" onClick={() => { setVisible(false); sessionStorage.setItem('miar-install-dismissed', '1'); }} aria-label="Fechar" className="rounded-lg px-2 py-1.5 text-slate-400">X</button></div></aside>; }
